// creates hexa wallet (different from bitcoin wallet)
// returns wallet entity

NewWallet = function(creator_id, type, picture_url, hex, members=[], transactions=[]) {

    // TODO

};

export default NewWallet;