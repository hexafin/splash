export const getGenericPassword = () => {
	return Promise.resolve("password");
};

export const setGenericPassword = (password, data) => {
	return Promise.resolve();
};

export const resetGenericPassword = () => {
	return Promise.resolve();
};
